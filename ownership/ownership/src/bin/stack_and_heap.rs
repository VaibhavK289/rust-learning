// Stack
// All stack data must be fixed and consistent size at the compile time.
// Rust stores them in stack at runtime.
//
//
//Reference size is constant.
//
//Reading data is faster in stack then heap.
//Purpose of ownership is to assign resoponsibility for deallocating memory[primarily heap memeory].
//
fn main() {}
