fn main() {}

//Concept Overview

// In Rust, drop refers to two distinct but tightly linked mechanisms:

// std::mem::drop (Function): An explicit call to end a value's lifetime immediately.

// Drop (Trait): A mechanism to define custom cleanup logic (destructors) when a value's lifetime ends.
